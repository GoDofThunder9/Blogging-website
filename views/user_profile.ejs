<link rel="stylesheet" type="text/css" href="/Profile.css">
<link rel="stylesheet" type="text/css" href="/home.css"/>
<% if(user.id == profile_user._id)
{%>
    <img  src="<%= user.avatar %>" alt="<%= user.name %>" height="100px" width="100px">
    <h3><%= profile_user.name %></h3>
    <button onclick="myFunction()">EDIT</button>
    <form id="FORM" action="/update/<%= profile_user.id %>" enctype="multipart/form-data" method="post">
      <input class="input" type="text" name="name" placeholder="your name" value="<%= profile_user.name %>" required>
      <input class="input" type="text" name="email" placeholder="your email" value="<%= profile_user.email %>" required>
      <input class="input" type="file" name="avatar" placeholder="profile_picture">
      <input class="submit" type="submit" value="update">
    </form>
    <div id="post-list-container">
      <ul>
   <% for( Post of Post)
   {%>
     <% if(Post.uuser._id == user.id)
     {%>
       <%- include('_post'); %>
       <%}
       %>
       <%} %>
     </ul>
 </div>
<%}
 else
 {%>
  <img  src="<%= profile_user.avatar %>" alt="<%= user.name %>" height="100px" width="100px">
  <h3><%= profile_user.name %></h3>
  <% for( Post of Post)
  {%>
    <div id="post-list-container">
      <ul>
        <% if(profile_user.id == Post.uuser._id)
        {%>
          
          <%- include('_post'); %>
          <%}
          %>
        </ul>
      </div>
      <%} %>
      <%}
      %>